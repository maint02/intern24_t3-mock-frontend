/**
 * The predefined label types - `value` represents the current display value,
 * trimmed to the third decimal, and `percentage` represents the
 * calculated percentage value based on the current display value, and the range
 * defined by `min` and `max`
 */
export declare type LabelType = 'value' | 'percent';
